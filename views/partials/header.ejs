<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Cryptron-app</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-gradient shadow-sm">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="/">✨ AuthApp</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          
          <% if (user) { %>
            <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
            <!-- ADDED: The Javascript confirmation dialog -->
            <li class="nav-item">
              <a class="nav-link text-warning fw-bold" href="/auth/logout" onclick="return confirm('Are you sure you want to logout?');">Logout</a>
            </li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link fw-bold" href="/auth/login">Login</a></li>
          <% } %>
          
        </ul>
      </div>
    </div>
  </nav>
  <div class="container py-5">